# 鹈鹕骑自行车 · 3D 场景

浏览器实时渲染的 3D 小品：一只鹈鹕骑着自行车，沿海边公路穿过晨雾。

- 纯前端，无构建步骤，无外部素材（几何、纹理、音乐全部程序生成）
- 依赖 three.js（CDN），其余代码本地

打开 `index.html` 需通过 http 服务（ES Module + 离线缓冲要求同源）：
`python -m http.server 8080` 后访问 http://localhost:8080

操作：鼠标拖拽旋转视角，滚轮缩放，点击画面开始（音频需用户手势）。
